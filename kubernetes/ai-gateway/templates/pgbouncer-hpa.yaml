{{- if and .Values.pgbouncer.enabled .Values.pgbouncer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ai-gateway.pgbouncer.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-gateway.pgbouncer.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ai-gateway.pgbouncer.serviceName" . }}
  minReplicas: {{ .Values.pgbouncer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.pgbouncer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.pgbouncer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.pgbouncer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.pgbouncer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.pgbouncer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
